{
  "_comment": "Версии сборочных инструментов SharpOS и форка CoreCLR — одни на Windows, macOS и Linux. Скрипты сборки ничего не ставят: находят инструменты (PATH или переменные окружения) и сверяют версии с этим файлом через tools/Toolchain.ps1. Ставит их mise bootstrap (mise.toml), см. README «Как запустить».",

  "llvm": {
    "version": "22.1.8",
    "_why": "Точная версия. clang 23 отвергает __try рядом с объектом, требующим раскрутки; clang 19 — no_builtin на defaulted-функции из заголовков MSVC. clang-cl, lld-link, llvm-lib и llvm-rc берутся из одного каталога LLVM.",
    "_where": "каталог bin LLVM в PATH (ищется clang-cl) или SHARPOS_LLVM_BIN"
  },

  "jwasm": {
    "version": "2.21",
    "commit": "7f6f32e78b79565d40bcce496756aadd1ff66900",
    "_why": "Ассемблер MASM-исходников форка на всех хостах. llvm-ml не умеет SECTIONREL (INLINE_GETTHREAD), ml64 есть только в Visual Studio. Версию проверяем по его выводу; commit — из какого дерева собирать.",
    "_where": "jwasm в PATH или SHARPOS_JWASM (путь к исполняемому файлу)"
  },

  "winsdk": {
    "sdkVersion": "10.0.26100",
    "crtVersion": "14.44.35220",
    "_why": "Заголовки и библиотеки Windows SDK и MSVC CRT, splat от xwin. Нужны только форку и ядру с форком; чистому ядру (-SkipCoreClr) и приложениям — нет. crtVersion — как в crt/include/crtversion.h; у xwin это --crt-version 14.44.17.14.",
    "_where": "SHARPOS_XWIN_SPLAT или .xwin-cache/splat в корне SharpOS"
  },

  "dotnet": {
    "sdkVersion": "10.0.105",
    "hostedRuntime": "10.0.5",
    "ilcompilerNet8": "8.0.27",
    "_why": "sdkVersion — точная версия для форка, её требует его Arcade (dotnet-runtime-sharpos/global.json), mise ставит именно её. Ядру и apps_native версия SDK безразлична: они NoStdLib, от SDK нужен только Roslyn с C# 14, а кодогенерацию задаёт ILCompiler (закреплён отдельно) — поэтому global.json SharpOS принимает любой 10.0.x. hostedRuntime — Microsoft.NETCore.App форка: под него собираются apps_managed, чтобы не компилироваться против ref-пакета новее рантайма, на котором они исполняются в SharpOS."
  },

  "minimum": {
    "_why": "На результат сборки не влияют; нужна не ниже указанной.",
    "cmake": "3.20",
    "ninja": "1.10",
    "python": "3.8"
  },

  "pwsh": {
    "version": "7.6.5",
    "_why": "PowerShell для гостя, payloads/pwsh/PowerShell-<версия>-win-x64. Мажорная версия .NET должна совпадать с рантаймом (10 → 7.6.x)."
  }
}
