# Phase 1 EH probe scan outputs (regenerable via probe_*.ps1).
unwind_scan.txt
eh_trailer_scan.txt

# Phase 2: dotnet-runtime fork (CoreCLR sources). Lives next to SharpOS for
# convenience but is a separate logical repo (eventual GitHub fork). Not
# committed to SharpOS — would add 5+ GB of upstream sources.
dotnet-runtime-sharpos/

## A streamlined .gitignore for modern .NET projects
## including temporary files, build results, and
## files generated by popular .NET tools. If you are
## developing with Visual Studio, the VS .gitignore
## https://github.com/github/gitignore/blob/main/VisualStudio.gitignore
## has more thorough IDE-specific entries.
##
## Get latest from https://github.com/github/gitignore/blob/main/Dotnet.gitignore

# Build results
[Dd]ebug/
[Dd]ebugPublic/
[Rr]elease/
[Rr]eleases/
x64/
x86/
[Ww][Ii][Nn]32/
[Aa][Rr][Mm]/
[Aa][Rr][Mm]64/
bld/
[Bb]in/
[Oo]bj/
[Ll]og/
[Ll]ogs/

# .NET Core
project.lock.json
project.fragment.lock.json
artifacts/

# ASP.NET Scaffolding
ScaffoldingReadMe.txt

# NuGet Packages
*.nupkg
# NuGet Symbol Packages
*.snupkg

# Others
~$*
*~
CodeCoverage/

# MSBuild Binary and Structured Log
*.binlog

# MSTest test Results
[Tt]est[Rr]esult*/
[Bb]uild[Ll]og.*

# NUnit
*.VisualState.xml
TestResult.xml
nunit-*.xml

# dotnet-home
**/.dotnet-home/

# qemu
**/.qemu/

# OVMF firmware binaries (build with .\ovmf\build.ps1)
ovmf/*.fd

# vs
**/.vs/

# GC research + legacy material — external reference projects, not part
# of the tree. old SharpOS lives at gc-experiment/old-SharpOS/.
gc-experiment/
# ILC --map outputs
**/map.txt
.claude/settings.local.json

# Transient build logs from build_clr_sharpos.ps1 runs (Tee-Object output).
build_log*.txt

# Vanilla CoreCLR clones for 3-way symbol audit reference baselines (huge,
# rebuilt on demand).
dotnet-runtime-vanilla-win/
dotnet-runtime-vanilla-wsl/

# Build/test output trees, transient logs & MSBuild caches — regenerable,
# never source. (f715a32 accidentally committed 838 of these — 334k lines,
# 75 MB pdb; see done/step068.md / memory feedback-never-commit-build-artifacts.)
work/
hello/
last_build.log
*.lscache
