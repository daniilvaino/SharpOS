# step 166 — проход по документам, последние ручные байты

Уборка после step165. Кода мало, документов много.

## Ассемблер: миграция закрыта

`Idt.Install` писала помощники `lidt`/`sidt` байтами (`0F 01 19 C3`,
`0F 01 09 C3`) — при том что векторные трамплины строкой выше уже шли через
генератор. И проба защиты страниц писала свою полезную нагрузку
(`mov eax,0x5A5A; ret`) тем же способом. Оба переехали в `BootAsm`; байты
сгенерированных шаблонов сверены с рукописными — совпадают.

Рукописных эмиттеров машинного кода в дереве больше нет. Осталось три
**сличающие базы** (`AppServiceBuilder`, `Cr3Accessor`, `ByRefAssignRefPatcher`):
Iced пишет в живое место, legacy — в черновик, расхождение = паника. Это не
долг, а проверка, и живёт она в комментариях у самого кода.

`docs/asm-inventory.md` удалён: это был план миграции, план выполнен.

Готча генератора: тело с **именованной константой** (`a.mov(eax, MarkerValue)`)
не просто не собирается — оно снимает генерацию у всех стабов сразу, и сыплется
`CS8795` по чужим файлам. Обходчик сворачивает литералы, не ссылки на `const`.

## Тихий режим для управляемых запусков

`RunManagedApp` (step165) не выставлял `Console.Quiet`, хотя загрузочная проба
делает это вокруг своего запуска. Из лаунчера диагностика ядра лезла в вывод
приложения. Закрыто тем же флагом `Probes.HostedAppQuietConsole`. **Прогоном не
проверено.**

## Документы

Живых наших `.md` — девятнадцать (остальные 179 из 198 — история `done/`,
вендорные и `work/`). Правил только то, что проверил по коду.

- **`CLAUDE.md`** — шесть устаревших утверждений: «последний шаг step115» (был
  165), Iced «без managed-делегатов» (работают с step131), «async пока нет»
  (работает), GC «консервативный обход» (точный с step110), «ELF-app tier»
  (нет с step137), «AVX/EVEX нет» (Vector128 работает). Добавлен раздел про
  вендоринг — правил не было, хотя в `vendor/` шесть библиотек.
- **`README.md`** — «Отдельное спасибо» смешивало чужой код в образе и тех, у
  кого мы учились. Разделено: «Вендоринг и библиотеки» (лицензии обязывают) и
  благодарности. Не хватало Terminal.Gui и TurboXml.
- **`docs/memory-ownership.md`** — пять ложных фактов, все в области, которую
  step165 и менял: страницы «не возвращаются» (возвращаются), пул приложения
  1 МиБ (64), ELF-стеки, перевёрнутый инвариант `ReclamationDisabled` (sweep
  включён с step110), консервативный обход. Добавлено про исчерпание пула —
  выделение отдаёт `null`, и отказ всплывает разыменованием.
- **`docs/eh-actual-map.md`**, **`eh-model.md`** — Tier B «EH нет» (PE-приложения
  делят движок ядра), cross-image walk отмечен сделанным, опкоды раскрутки
  Tier A обновлены.
- **`docs/pal-minimal-audit.md`** — шапка со статусом по коду форка: семейство
  `vsprintf` закрыто (`vsnprintf_impl`), `_wassert` и `vsscanf` открыты.
- **`limits.md`** — дата, причина отказа `Process.Start` (`CreateProcessW`, а не
  `SigChld`), актуальное состояние трёх ярусов и открытых фронтов.
- **`vendor/Iced/PROVENANCE.md`** — создан; был единственным без него, а README
  обещал его у каждой библиотеки.
- Пути `\EFI\BOOT` → `\apps` в `payloads/README.md` и `boot-order.md`.

## Не сделано

`docs/boot-order.md` — скелет фаз верен, наполнение апрельское: 7 проб из 80,
«ELF validation и DemoApp», «CR3 пока firmware» (активирован в step94), раздел
«будущие фазы» перечисляет давно сделанное. Это пересборка по `BootSequence.cs`,
не правка строк.

## Чего стоила точность

Про остаток ручных эмиттеров я трижды дал неверную цифру: сначала «82 записи»
негодным грепом (он ловил значения регистров RTC и команды SCSI), потом «по сути
завершена» вместо сверки с критерием приёмки, потом свалил в одну строку данные
устройства и шесть байт инструкций. Каждый раз проверка стоила одной команды.
